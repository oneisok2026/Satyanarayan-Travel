/**
 * Test stub for the `server-only` package.
 *
 * The real module throws on import outside a Server Component. Next.js still
 * enforces that at build time; this stub only exists so Vitest can unit-test
 * server modules directly.
 */
export {};
